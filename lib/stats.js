// Generated by CoffeeScript 1.3.3
(function() {
  var Stats, StatsD, os;

  StatsD = require('node-statsd').StatsD;

  os = require('os');

  Stats = (function() {

    function Stats(host, port, prefix) {
      this.host = host;
      this.port = port;
      this.prefix = prefix;
      this.stats = new StatsD(this.host, this.port);
      this.hostname = os.hostname();
    }

    Stats.prototype.inc = function(m, v) {
      return this.stats.increment(this.pref(m), v);
    };

    Stats.prototype.pref = function(m) {
      return "" + this.prefix + "." + m + "." + this.hostname;
    };

    return Stats;

  })();

  module.exports = Stats;

}).call(this);
